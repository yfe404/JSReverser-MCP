# Tool Parameter Defaults
- search_in_scripts: sign|token|nonce|encrypt|hmac|sha|md5|cookie|h5st
- get_hook_data: view=summary, maxRecords=80
- First round Hook: fetch + xhr
- record_reverse_evidence: channel=runtime-evidence
- export_rebuild_bundle: prioritize exporting env/entry.js + env/env.js + env/polyfills.js + env/capture.json
- Breakpoints are disabled by default, only enabled as a fallback
