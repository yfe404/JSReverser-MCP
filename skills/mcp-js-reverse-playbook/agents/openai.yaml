interface:
  display_name: "MCP Frontend Reverse Engineering Specification"
  short_description: "Frontend JS reverse engineering: environment patching, instrumentation, deobfuscation"
  default_prompt: "Use $mcp-js-reverse-playbook to reverse engineer the signing chain of the target site, following the hook-first workflow to complete environment patching, instrumentation, and evidence-based output."

policy:
  allow_implicit_invocation: true
